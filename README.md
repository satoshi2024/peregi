-- 1. 更新主表：设为 2（程序里会映射为 1），并把更新时间刷新到当前，确保被抽出
UPDATE ZABTGANTAN 
SET 
    JUGAI_KBN = '2', 
    SAKUJO_FLG = '0',
    SYS_KOSHINBI = TO_NUMBER(TO_CHAR(SYSDATE, 'YYYYMMDD')), 
    SYS_JIKAN = TO_NUMBER(TO_CHAR(SYSDATE, 'HH24MISS'))
WHERE NENDO = '2026' AND KOJIN_NO = '1015340440';

-- 2. 删除关联表：确保去查代码时触发 NULL
DELETE FROM ZABTJUGAIKANRI 
WHERE KOJIN_NO = '1015340440';

-- 3. 提交事务
COMMIT;
