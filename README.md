从您提供的照片来看，Excel 的第 6 行正是测试用例的表头（列名）。结合之前我们修复的 QA #28652 Bug（SORT_KEY5 在不按重量排序时被错误赋值了张数），本次测试的核心目标是验证重量排序的开关是否正常生效，且不影响原有的默认排序。
参照表格中已有的书写规范（如“正常終了すること”、“～出力されていること”、“★現新比較”等），我为您拟定了本次 Bug 修复对应的 2 条核心测试用例内容。您可以直接参考并填入 Excel 中：
本次 QA #28652 对应的测试用例（建议追加 2 行）
| No | 種別 | アクション・バッチ処理条件 (测试执行条件) | 出力帳票/マスタ (输出对象) | 確認内容 (预期结果/确认点) | テスト仕様書 変更履歴 |
|---|---|---|---|---|---|
| (新No.1) | 11 12 | 特徴当初帳票作成処理を実行する。
（重さ順を指定しないで実行） | 特徴通知書
（※根据实际影响范围写个人/事业所） | 正常終了すること。
特徴通知書の並び順が、重さ（枚数）の影響を受けず、指定した通常の並び順で正しく出力されていること。
（★現新比較） | QA#28652対応 2026/4/1追加 |
| (新No.2) | 11 12 | 特徴当初帳票作成処理を実行する。
（重さ順を指定して実行） | 特徴通知書
（※根据实际影响范围写个人/事业所） | 正常終了すること。
特徴通知書が、重さ順（枚数が考慮された順序）で正しく出力されていること。
（★現新比較：過去の正常な重さ順のデータと比較） | QA#28652対応 2026/4/1追加 |
💡 为什么设计这两个 Case？
 * 新 No.1 (不指定重量顺)：【Bug 验证】 这是针对本次工单直接反馈的问题。修改了 ELSE 分支（或置空逻辑）后，必须验证在不勾选重量排序时，SORT_KEY5 不会再携带“张数”，报表的排序恢复到正确的默认顺序。
 * 新 No.2 (指定重量顺)：【回归测试 Regression】 我们为了修 Bug 加上了 IF BIT5 = 1 THEN 的条件，为了防止“修好了一头，搞坏了另一头”，必须验证在正常勾选重量排序时，报表依然能正确按照重量和张数排序。
书写细节提示：
 * 截图右上角的“更新日”已经是 2026/4/1（今天），“更新者”是张（Zhang）桑，说明文档正在更新中。您在“変更履歴”那一列直接写上对应工单号 QA#28652 即可，这符合日企严谨的追溯习惯。
 * 截图里有很多 ★現新比較（新老版本对比测试），对于排序问题，现新对比是最直观的确认方法。保留这个确认基准能省去很多沟通成本。
