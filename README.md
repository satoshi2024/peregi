太棒了！张工，恭喜你拿下了最难啃的“给与画面（ZAB021S011）”！
现在我们转战**“年金画面（ZAB021S012）”。这个画面的问题和上一个如出一辙**，完全是“同一个模子刻出来的 Bug”。
因为年金画面的代码当初大概率是从给与画面复制过去的，所以犯了完全相同的 == 2025 错误。但在年金画面里，修改起来反而更简单，因为它的复选框代码没有前后写两遍。
结合你发来的代码截图和最后完美的 2025 年效果图，请在 ZAB021S012.jsp 中精准完成以下 3 步替换（共 6 处），排版瞬间就能恢复正常：
🛠️ 终极修复方案（针对 ZAB021S012.jsp）
1️⃣ 第一处：修复大表头的跨列逻辑（colspan）
(参考你的图 24，大约在第 1531 - 1537 行)
导致整个表格骨架错位的元凶依然是它。到了 2026 年，系统判断不是 2025，就退回了跨 2 列，直接崩盘。
• 第 1531 行附近：
将 <c:if test="${ZAB021S012_VIEW.nendobun != '2025'}">
👉 修改为：<c:if test="${ZAB021S012_VIEW.nendobun <= '2024'}">
• 第 1537 行附近：
将 <c:if test="${ZAB021S012_VIEW.nendobun == '2025'}">
👉 修改为：<c:if test="${ZAB021S012_VIEW.nendobun >= '2025'}">
2️⃣ 第二处：修复“非居住”这三个字的表头显示
(参考你的图 25，大约在第 1601 行和 1620 行)
因为判断写死了 2025，所以到了 2026 年，表头的文字直接消失了！
• 第 1601 行附近（控配的非居住文字）：
将 <c:if test="${ZAB021S012_VIEW.nendobun == '2025'}">
👉 修改为：<c:if test="${ZAB021S012_VIEW.nendobun ge '2025'}">
• 第 1620 行附近（同配的非居住文字）：
将 <c:if test="${ZAB021S012_VIEW.nendobun == '2025'}">
👉 修改为：<c:if test="${ZAB021S012_VIEW.nendobun ge '2025'}">
3️⃣ 第三处：修复真正的复选框（Checkbox）显示
(参考你的图 28/29，大约在第 1723 行和 1744 行)
复选框也被套在了 2025 专属的条件里，导致 2026 年复选框凭空蒸发，右边的格子全部向左挤压，引发错位。
• 第 1723 行附近（控配非居复选框 kotaihai_haihikyo_kbn）：
将 <c:if test="${ZAB021S012_VIEW.nendobun == '2025'}">
👉 修改为：<c:if test="${ZAB021S012_VIEW.nendobun ge '2025'}">
• 第 1744 行附近（同配非居复选框 doupai_haihikyo_kbn）：
将 <c:if test="${ZAB021S012_VIEW.nendobun == '2025'}">
👉 修改为：<c:if test="${ZAB021S012_VIEW.nendobun ge '2025'}">
(注：JS 部分你已经在图 19 的第 604 和 614 行改成了 ge，改得非常棒，不用再动了。)
🚨 为什么说“代码没生效”？
这是 Java Web 老项目最爱捉弄人的地方！你刚才提到的“代码没生效”，100% 是 Tomcat 的本地缓存（Work 目录里的编译后 .class 文件）在搞鬼。
请按照以下严格步骤操作：
1. 保存 刚刚修改好的 ZAB021S012.jsp。
2. 在你的 VSCode / Eclipse 中，彻底停止 Tomcat 服务器。
3. 点击 Clean / Publish（清理服务器缓存）。
4. 重新启动 Tomcat。
5. 在浏览器里按 Ctrl + F5（强制刷新）。
改完这 6 行代码并强刷缓存后，年金画面绝对能乖乖变成你最后一张图里完美的模样！马上就全线通关了，加油冲！
