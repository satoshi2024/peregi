太棒了！你的第 25 张图（Before / After 对比图）做得非常标准，这在日本的 IT 测试交付中是满分的“エビデンス（证据）”。
根据你第 24 张图的 Excel 模板（IT1_処内_画外_ZAB021S001_個人毎課税資料一覧.xlsx），我为你编写了可以直接复制粘贴到 Excel 里的测试用例（テストケース / Test Case）。
正如你所要求的，我分为**【入力チェック（输入检查）】**和**【合算処理（合算处理）】**两本（两个 Sheet/部分），并且每个番号对应一个独立的 Case。
### 📝 第一本：入力チェック（输入检查）式样书
**前提**：这部分属于 課税台帳異動 画面的测试。针对工单要求废弃的所有错误消息进行“不再显示”的验证。
| No | 種別 | 遷移元画面 | アクション・条件 | 遷移先画面/帳票 | 確認内容 |
|---|---|---|---|---|---|
| 001 | IT1 | 課税台帳異動 | 指定番号を未入力、受給者番号を入力した状態で、「入力チェック」ボタンを押下する。（MSG: 11023相当） | 課税台帳異動 | 該当のエラーメッセージ（指定番号がないとき...）が表示されないこと。 |
| 002 | IT1 | 課税台帳異動 | 分離山林の所得金額に0以下の値を入力し、「入力チェック」ボタンを押下する。（MSG: 11058相当） | 課税台帳異動 | 該当のエラーメッセージ（山林所得がマイナス...）が表示されないこと。 |
| 003 | IT1 | 課税台帳異動 | 短期譲渡（一般）の所得金額に0以下の値を入力し、「入力チェック」ボタンを押下する。（MSG: 11071相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 004 | IT1 | 課税台帳異動 | 短期譲渡（軽減）の所得金額に0以下の値を入力し、「入力チェック」ボタンを押下する。（MSG: 11072相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 005 | IT1 | 課税台帳異動 | 長期譲渡（一般）の所得金額に0以下の値を入力し、「入力チェック」ボタンを押下する。（MSG: 11073相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 006 | IT1 | 課税台帳異動 | 長期譲渡（優良）の所得金額に0以下の値を入力し、「入力チェック」ボタンを押下する。（MSG: 11074相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 007 | IT1 | 課税台帳異動 | 長期譲渡（居住）の所得金額に0以下の値を入力し、「入力チェック」ボタンを押下する。（MSG: 11075相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 008 | IT1 | 課税台帳異動 | 有価証券（一般）の所得金額に0以下の値を入力し、「入力チェック」ボタンを押下する。（MSG: 11076相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 009 | IT1 | 課税台帳異動 | 有価証券（上場）の所得金額に0以下の値を入力し、「入力チェック」ボタンを押下する。（MSG: 11077相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 010 | IT1 | 課税台帳異動 | 長期譲渡（一般）の特別控除額に1,000,000を超える値を入力し、「入力チェック」を押下する。（MSG: 11088相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 011 | IT1 | 課税台帳異動 | 長期譲渡（優良）の特別控除額を入力し、条文を未入力として「入力チェック」を押下する。（MSG: 11113相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 012 | IT1 | 課税台帳異動 | 長期譲渡（居住）の特別控除額を入力し、条文を未入力として「入力チェック」を押下する。（MSG: 11114相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 013 | IT1 | 課税台帳異動 | 平成15年度以前のデータにて、配当割または株式割を入力し、「入力チェック」を押下する。（MSG: 11320相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。※画面再現不可の場合は机上確認 |
| 014 | IT1 | 課税台帳異動 | 異動事由補足に20桁を超える文字列を入力し、「入力チェック」を押下する。（MSG: 11341相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。※入力不可の場合は机上確認 |
| 015 | IT1 | 課税台帳異動 | 所得税の寄附金に95,000を超える値を入力し、「入力チェック」を押下する。（MSG: 11395相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 016 | IT1 | 課税台帳異動 | 合計所得金額と、各所得（分離所得は除く）の合計値が一致しない条件で「入力チェック」を押下する。（MSG: 11483相当） | 課税台帳異動 | 該当のエラーメッセージ（ZAB11483-W）が表示されないこと。 |
| 017 | IT1 | 課税台帳異動 | 生活保護区分=1のデータで、強制課税区分を未選択とし「入力チェック」を押下する。（MSG: 11570相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 018 | IT1 | 課税台帳異動 | 生活保護区分=1のデータで、強制課税区分を[1]とし「入力チェック」を押下する。（MSG: 11571相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
| 019 | IT1 | 課税台帳異動 | 生活保護区分=1のデータで、強制課税区分を[2等]とし「入力チェック」を押下する。（MSG: 11572相当） | 課税台帳異動 | 該当のエラーメッセージが表示されないこと。 |
*(注：表格中的 11320 和 11341，我加上了 ※画面再現不可の場合は机上確認，意思是如果画面无法操作，则通过代码 Review 确认，这也是非常标准的日式处理手法。)*
### 📝 第二本：合算処理（合算处理）式样书
**前提**：这部分属于 個人毎課税資料一覧 画面的测试。针对 9047 号错误消息进行验证。
| No | 種別 | 遷移元画面 | アクション・条件 | 遷移先画面/帳票 | 確認内容 |
|---|---|---|---|---|---|
| 001 | IT1 | 個人毎課税資料一覧 | 住宅借入金等特別控除関連項目（居住年月日など）が入力されている「給与支払報告書」データを2件以上選択し、画面左下の「合算」ボタンを押下する。（MSG: 9047相当） | 個人毎課税資料一覧 | エラーメッセージ「9047: 複数の給報に住借関連項目の入力がある...」が表示されず、合算処理が続行（または現行の正しい仕様に準拠した動作）されること。 |
### 💡 提交建议
 1. 将上述表格的日文内容直接复制到你的 Excel 测试用例模板中对应的列里。
 2. 在执行时，把你的 **Before / After 截图** 贴在证据栏（エビデンス欄）或附加的文件里。
 3. 对于那两个无法重现的 Case（11320 和 11341），可以在结果栏（結果）写上：画面入力不可のため、ソースコード上のロジック削除を机上確認済。（因画面无法输入，已在桌面评审确认源代码逻辑已删除）。
这样一份极其专业的测试式样书就大功告成了！你可以直接收尾提交了。
