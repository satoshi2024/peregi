看来小籔前辈终于把规格彻底确认清楚了！原来最开始那张图里的 1,2 才是对的，昨天我们线下以为的 0,1 确实是理解偏差。
前辈明确指示：**只有当参数是 1 或 2 时走新逻辑；如果是 0、NULL 或没有数据，都走老逻辑。**
好消息是，我们之前做的**代码重构（把取参数放到 PROC_INIT、加全局变量兜底 NULL）结构上是 100% 完美且正确的**，完全不需要动！你只需要把主循环里的判断条件改回 1, 2 即可。
### 🛠️ 具体的修改点（只有两处）
请回到主循环里（大约 1994 行附近），把你之前写的 IN ('0', '1') 改成 **IN ('1', '2')**。
修改后的代码如下：
```plsql
        --2026/05/12 S.Cyo Add start 1.1.307.000:保守QA28908対応
        -- ★ 这里把 ('0', '1') 改成 ('1', '2')
        IF V_CHG_KYOSEI_KAZEI_FLG IN ('1', '2') AND NVL(RDATA.KYOSEI_KAZEI_KBN, 0) = 4 THEN
            -- 強制課税区分 = 4 (租税条約（非課税）) の場合
            R_ZAAW010o008WK.KAZEIHIKBN := '0';    -- 非課税
            R_ZAAW010o008WK.HIKAZEI_KBN := '08';  -- 租税条約非課税

        -- ★ 这里也把 ('0', '1') 改成 ('1', '2')
        ELSIF V_CHG_KYOSEI_KAZEI_FLG IN ('1', '2') AND NVL(RDATA.KYOSEI_KAZEI_KBN, 0) = 5 THEN
            -- 強制課税区分 = 5 (租税条約（免税）) の場合
            R_ZAAW010o008WK.KAZEIHIKBN := '1';    -- 課税
            R_ZAAW010o008WK.HIKAZEI_KBN := '00';  -- 非該当 (均等割・所得割ともに課税)

        ELSE
        --2026/05/12 S.Cyo Add end

```
### 💡 为什么这样改就完美契合了前辈的要求？
 * 如果参数表配的是 1 或 2 👉 命中 IN ('1', '2')，走新逻辑。
 * 如果参数表配的是 0 👉 0 不在 ('1', '2') 里，条件不成立，掉进 ELSE 走老逻辑（既存通り）。
 * 如果没有配置（レコードなし）👉 我们的 PROC_INIT 会给变量赋 NULL，NULL 也不在 ('1', '2') 里，条件不成立，掉进 ELSE 走老逻辑（既存通り）。
### 💬 改完之后如何回复前辈：
你在数据库里改完这几个字，重新 Commit（提交）资产后，可以直接在 Teams 这样回复他：
小籔さん
ご連絡ありがとうございます。仕様承知いたしました。
該当箇所の判定条件を IN ('1', '2') に修正し、パラメータが 0 や NULL 等の場合は既存ロジックを通るよう対応しました。
先ほど修正版を再コミットいたしましたので、お手すきの際にご確認をお願いいたします。
这次是真的改到彻底完美了！不用担心，这种规格的反复在开发中太常见了，改掉这两个数字就大功告成！
