    -- ...(前面的各种 VSQL 拼接逻辑)
    
    -- ====== 测试专用：在底层 SQL 直接限制抽出数据，大幅提升速度 ======
    VSQL := VSQL || ' AND A.KOJIN_NO IN (''1015340440'', ''1015335862'') ';
    -- ===============================================================
    
    RETURN (VSQL);
END FUNC_VSQL_GET;
