既然你已经修复了代码中的作用域问题，我们需要针对这个具体的 Bug（QA #28351）增加一条**回归测试（リグレッションテスト）**，以确保两人同时申请时，发行番号能正确复制。
你可以按照以下格式将其追加到你的测试式样书中：
### 追加テストケース（QA #28351 故障修正の確認）
| No | 遷移元画面 | アクション・条件 | 遷移先画面/帳票 | 確認内容 |
|---|---|---|---|---|
| **004-001** | 証明書発行画面 | **【複数人同時発行の確認】**
世帯内で**2名以上**の対象者を選択し、課税・非課税証明書の印刷プレビュー（または発行）を実施する。 | 即時帳票発行履歴画面 / データベース（ZABTPRINTLOG_ONL） | ① 選択した**全員分**のレコードが正しく登録されていること。
② **全員分（2人目以降も含む）**の「発行番号」に、1人目と同じ正しい番号がセットされていること。 |
### 💡 为什么这样写？（给你的建议）
 * **测试重点**：由于之前的 Bug 是因为循环逻辑导致第二个人的 hakoBangoArr 为空，所以确认内容里必须强调 **「2人目以降」** 也拿到了正确的番号。
 * **检查项（检查点）**：
   * 如果你本地环境起不来，无法看页面效果，建议在确认内容里加上 **数据库表名（ZABTPRINTLOG_ONL）**。这样你可以让 QA 或者 DB 同事直接查表，确认 hakkoBango 字段是否不再为 null。
 * **数据准备**：测试时请确保 hastuBusu（份数）至少为 1，这样才能触发你修改的那段循环逻辑。
你可以直接把这一行贴到你的 Excel 里的“変更履歴”或者新的测试 Sheet 中。祝你测试一次性通过！
