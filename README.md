太好了！既然底层数据库（或者提供数据的 View 所依赖的源表）里都有这些字段，那你的测试准备工作就非常轻松了。
在日企的这种后台批处理（Batch）或接口（連携）开发中，为了精准覆盖特定的 IF/ELSE 分支，最标准、最高效的做法就是**直接写 SQL 去 UPDATE 测试库里的数据**，人为“造”出满足条件的数据状态。
为了让你明天能以最快速度完成测试并截图交差，我连 UPDATE 语句的模板都帮你写好了，你只需要替换一下**表名**和**测试人员的 ID** 就能直接跑：
### 🛠️ 测试数据准备 SQL 模板
假设你的底层数据源表叫 [あなたの課税情報テーブル]，用来标识纳税人的主键是 [宛名番号/個人番号]。
#### Case 006-001：制造“全额减免，且原本会产生尾数”的数据（测试 IF 分支生效）
你需要找一个专门用来测 QA29912 的测试人员 A（千万别和 QA28908 的人混用）：
```sql
-- 造数据：均等割完全抵消，且故意设置端数(尾数)为 100
UPDATE [あなたの課税情報テーブル]
SET 
    -- 【关键点1：均等割抵消为0】
    SHIM_KINTOWARI = 5000,        -- 市民税均等割
    SHIM_GENMEN_KINWARI = 5000,   -- 市民税均等割减免
    KEN_KINTOWARI = 5000,         -- 県民税均等割
    KEN_GEN_KINTOWARI = 5000,     -- 県民税均等割减免
    
    -- 【附加：所得割也设为抵消，为了严谨】
    SHIM_SHOTOKUWARI = 10000,     
    SHIM_GENMEN_SHOWARI = 10000,  
    KEN_SHOTOKUWARI = 10000,      
    KEN_GEN_SHOWARI = 10000,      
    
    -- 【关键点2：必须有端数！】
    SHIM_HASU = 100,              -- 市民税端数
    KEN_HASU = 100                -- 県民税端数
WHERE 
    [宛名番号/個人番号] = 'テスト用ID_A';

COMMIT; -- 别忘了提交

```
👉 **执行批处理后，去查 ZAAW010o008WK 表，预期结果：** 测试人员 A 的 SHIM_SHOTOKUWARI 和 KEN_SHOTOKUWARI 必须都是 **0** （那个100的尾数被你的代码成功拦截并清零了）。
#### Case 006-002：制造“普通需缴税，且有尾数”的数据（测试 ELSE 分支正常运作）
再找另一个测试人员 B：
```sql
-- 造数据：有均等割要交，走原本的带尾数计算逻辑
UPDATE [あなたの課税情報テーブル]
SET 
    -- 【关键点1：均等割没有抵消为0】
    SHIM_KINTOWARI = 5000,        
    SHIM_GENMEN_KINWARI = 0,      -- 减免为0，差值是 5000
    KEN_KINTOWARI = 5000,         
    KEN_GEN_KINTOWARI = 0,        
    
    -- 【设定所得割】
    SHIM_SHOTOKUWARI = 10000,     
    SHIM_GENMEN_SHOWARI = 2000,   -- 差值是 8000
    KEN_SHOTOKUWARI = 10000,      
    KEN_GEN_SHOWARI = 2000,       
    
    -- 【关键点2：同样设置端数】
    SHIM_HASU = 100,              
    KEN_HASU = 100                
WHERE 
    [宛名番号/個人番号] = 'テスト用ID_B';

COMMIT;

```
👉 **执行批处理后，去查 ZAAW010o008WK 表，预期结果：** 测试人员 B 的 SHIM_SHOTOKUWARI 和 KEN_SHOTOKUWARI 必须都是 **8100**（(10000 - 2000) + 100，证明你没破坏系统原本加端数的正常逻辑）。
### 💡 测试截图 (エビデンス) 建议：
 1. **执行前 (事前)：** 截一张上面那两个 SQL 执行完毕后，数据库源表的 SELECT 结果截图。
 2. **执行后 (事後)：** 跑完你们那个“個人住民税課税情報提供”的处理后，截一张中间工作表（ZAAW010o008WK）的 SELECT 结果，用红框把 ID_A 的 0 和 ID_B 的 8100 框出来。
 3. **日志 (Log)：** 按照你式样书里的 005-001 Case，截一张处理正常终了的画面日志。
这套组合拳打下来，证据链非常完美，Leader 或测试复核的人绝对挑不出毛病。加油！
