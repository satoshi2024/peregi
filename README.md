张工，我仔细看了你最新发来的这 4 张截图。你找的位置非常准确！
现在我完全明白为什么你的画面会彻底崩盘了：**这是因为 JSP 的判断条件写得有重叠，导致在令和 8 年（2026年）时，同一个复选框被画了两次，直接把表格撑爆了！**
下面是为你量身定制的**保姆级修改方案**，只要照着改，排版绝对瞬间恢复完美：
### ✅ 第 5 处：你改得非常完美！（图4）
先表扬一下，你的**最后一张截图（图4，第 554-569 行）**改得完全正确！你把 == '2025' 注释掉，换成了 ge '2025'，JS 逻辑已经彻底修复，这部分**不需要再动了**。
### 🛠️ 第 2 处：修改大表头跨列逻辑（图1）
**【问题原因】**：目前代码写的是 != '2025'（跨2列）和 == '2025'（跨3列）。如果你选了 2026 年，它会走 != '2025' 的分支，变成跨 2 列，直接错位！
**【修改方法】（参考图1）：**
 * **第 1434 行**：
   将 <c:if test="${ZAB021S011_VIEW.nendobun != '2025'}">
   👉 **修改为**：<c:if test="${ZAB021S011_VIEW.nendobun <= '2024'}">
 * **第 1440 行**：
   将 <c:if test="${ZAB021S011_VIEW.nendobun == '2025'}">
   👉 **修改为**：<c:if test="${ZAB021S011_VIEW.nendobun >= '2025'}">
### 🛠️ 第 3 处：修改小表头文字逻辑（图2）
**【问题原因】**：和上面一样，!= 和 == 会导致 2026 年时，“老人”变成全称，或者干脆消失。
**【修改方法】（参考图2）：**
 * **第 1506 行 & 第 1529 行**：
   将 <c:if test="${...nendobun != '2025'}">
   👉 **修改为**：<c:if test="${ZAB021S011_VIEW.nendobun <= '2024'}">
 * **第 1511 行 & 第 1533 行**：
   将 <c:if test="${...nendobun == '2025'}">
   👉 **修改为**：<c:if test="${ZAB021S011_VIEW.nendobun >= '2025'}">
### 🛠️ 第 4 处：致命错误！解决复选框重复渲染（图3）
**【问题原因】**：这是排版错乱的**核心元凶**！
在图 3 中，最下面的第 1592 行是一个 >= 2025 的块，里面已经包含了**所有 6 个**复选框（顺序是正确的）。
但是！上面的第 1564 行（!= 2025）和第 1578 行（>= 2019），在 2026 年时**全都是成立的（True）**！
这就导致系统先把旧的 4 个框画了一遍，紧接着又把新的 6 个框画了一遍，你的表格里塞了 10 个复选框，不乱才怪！
**【修改方法】（参考图3）：我们要让上面旧的框在 2025 年及以后彻底闭嘴！**
 * **第 1564 行**：
   将 <c:if test="${ZAB021S011_VIEW.nendobun != '2025'}">
   👉 **修改为**：<c:if test="${ZAB021S011_VIEW.nendobun <= '2024'}">
 * **第 1578 行**（限制同配旧逻辑的年份）：
   将 <c:if test="${ZAB021S011_VIEW.nendobun >= '2019'}">
   👉 **修改为**：<c:if test="${ZAB021S011_VIEW.nendobun >= '2019' && ZAB021S011_VIEW.nendobun <= '2024'}">
### 💡 总结
你不需要再去剪切/移动任何 <td> 标签了！因为我发现你代码底部那个 >= 2025 的代码块（第 1592 行往下）里面的 6 个复选框顺序已经是完美正确的了。
**你只需要把上述 6 个 <c:if> 的年份判断条件，严格划分为 <= 2024（旧版）和 >= 2025（新版），排版就会瞬间恢复正常！**
快去修改这几个判断条件，重启测试，成功就在眼前！
